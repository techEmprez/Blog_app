<div>
   <div>
     <img src=<%= user.photo %> alt="profile picture of <%= user.name %>" />
   </div>
    <div>
      <h2><%= user.name %></h2>
      <p>Number of Posts: <%= user.posts_counter %></p>
      <p><%= link_to 'View', user_path(user.id)%></p>
    </div>
</div>
